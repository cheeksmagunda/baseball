# Project runtime dependencies.
# Source of truth for versions is pyproject.toml; this file mirrors the
# resolved set so `pip install -r requirements.txt` works on any platform
# (the previous contents were a full `pip freeze` from an Ubuntu host and
# included system-only packages like dbus-python, conan, launchpadlib, etc.).
#
# To regenerate: pip install pip-tools && pip-compile pyproject.toml -o requirements.txt

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# DB + ORM
sqlalchemy>=2.0
alembic>=1.13

# Config + validation
pydantic>=2.0
pydantic-settings>=2.0

# HTTP clients
httpx>=0.27
requests>=2.32

# Cache
redis[hiredis]>=5.0

# Data / math
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
joblib>=1.4

# Statcast kinematics (Baseball Savant → pitch physics + exit velocity)
pybaseball>=2.2

# Retry logic
tenacity>=8.2

# Security (keep current — older 41.x has known CVEs)
cryptography>=45.0

# Dev
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
